# 换行符归一化。
#
# 这个仓库主要在 Windows 上开发、在 Linux 上构建运行。没有这份声明的话，
# Git 的 core.autocrlf 行为会随每台机器的配置而变：同一份 Go 源码，
# 有人提交成 CRLF、有人提交成 LF，diff 里就会出现整文件「改动」的噪音。
#
# `text=auto eol=lf` 的含义是：仓库里一律存 LF，检出时也还原成 LF。
# 对 Go / TypeScript / YAML 这些跨平台工具链来说，LF 是唯一安全的选择 ——
# 尤其 YAML，CRLF 会让某些解析器把 \r 当成内容的一部分。
* text=auto eol=lf

# 二进制文件必须显式排除，否则 Git 会尝试「归一化」它们而损坏内容
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
*.ttf binary

# 数据库与压缩包同理
*.db binary
*.db-wal binary
*.db-shm binary
*.zip binary
*.gz binary
*.exe binary

# 这两个文件的换行符**必须是 CRLF**，写成 LF 后 Windows 的 cmd.exe
# 无法正确执行（会把整行当成一个命令名）。
*.bat text eol=crlf
*.cmd text eol=crlf

# 明确 Go 模块文件的语言，让 GitHub 的语法高亮与 diff 更准确
go.mod linguist-language=Go
go.sum linguist-language=Go
